pi case monitor
pi
**r**k*

On thhe pi (piMon):
cd /etc/xdg/lxsession/LXDE-pi 
sudo nano autostart

then edit the url address

Chromium startup:
@chromium-browser --kiosk --disable-restore-session-state http://ruairipc/hardmon


to build production version of Angular app:

ng build --prod --base-href "./"

